# 2026-08-18 - Monitoring quotidien

Rapport complet : commentaire du 2026-08-18 sur l'issue #143. PR de l'execution : celle-ci.

Diagnostic vert sur `main` (`8200794`, inchange depuis le 2026-08-11, huitieme run
consecutif sur le meme arbre) : `lint`, `typecheck` et `test` (186 fichiers, 1614
tests, 1 ignore) passent. `npm audit --omit=dev` : les memes 6 avis `high`
(`brace-expansion`, `fast-uri`, `sharp` via libvips) que les jours precedents,
deja portes par le rapport du 2026-08-08 sur #143, non re-remontes.

Note d'exploitation : six PR de routine restent ouvertes et non mergees (#157,
#158, #159, #162 du monitoring, #160 de l'audit hebdomadaire, #164 sur le cout des
routines). Les fragments de journal des 2026-08-12, 13, 14 et 17 ne vivent donc que
dans ces branches ; ils ont ete lus directement depuis
`origin/claude/monitoring-2026-08-1{2,3,4,7}` pour recharger la memoire, la seule
lecture de `main` l'aurait manquee. C'est le huitieme run consecutif sans merge :
le dossier `journal/` de `main` ne contient toujours qu'un seul fragment.

Methode : 3 sous-agents chasseurs en parallele sur les zones les moins couvertes par
les runs precedents (calcul monetaire, module Projet, integrations et crons), puis un
sous-agent verificateur par constat, charge de le REFUTER avec consigne de conclure
REFUTE en cas de doute. **10 constats candidats, dont 2 ecartes avant verification
comme deja connus, 8 soumis a verification : 5 confirmes et 3 refutes.**

## Constats confirmes, remontes sur l'issue #143

| Constat                                                                                                                                                                                                                                                                                                                                                                                                                                                | Decision | Raison                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `loadAvoirsCredit` (`lib/echeancier/ajustements.ts:88-112`) somme TOUS les avoirs portant `contrat_id`, sans filtrer sur la cause, alors que `loadBilledLines` (`:41-45`) filtre strictement. `computeDerivance` (`lib/echeancier/calc.ts:443-451`) faisant `deltaNet = deltaBrut - creditsExisting` avec un credit NEGATIF (`avoirs.ts:369`, `montantHt = -Math.abs(montant)`), un avoir emis pour une autre cause est REAJOUTE au montant a facturer | reportee | Confirme en verification adversariale, chiffres refaits avec les vrais signes. Contrat NPEC 12000 -> 14000, taux 10 %, jalon 0,25, facture 300,00 HT, avoir « Remise commerciale » de -300,00 : deltaBrut +50,00, creditsExisting -300,00, deltaNet = +350,00. L'ajustement propose « facture complementaire de 350,00 EUR HT » au lieu de 50,00, soit la remise commerciale refacturee au client. Les tests `__tests__/echeancier-calc.test.ts:350` et `:433` ne pinnent que la branche `delta_ht_brut` NEGATIF, ou soustraire un credit negatif REDUIT l'avoir (anti sur-credit voulu) ; personne ne couvre la branche positive, ou le meme calcul produit l'inverse. Attenuation : l'ajustement est `pending` et valide a la main. Non corrige en PR automatique : touche une formule de facturation, exclue par le prompt de la routine. Distinct du constat du 2026-08-17 sur le meme fichier, qui portait sur l'absence de filtre de STATUT.                                       |
| L'avoir de rupture propose 0,00 EUR sur toute facture event-based (modele `engagement`, HEOL) : `loadBilledLines` filtre `.is('event_type', null)` (`lib/echeancier/ajustements.ts:42`) alors que `createFactureFromEvents` ecrit `event_type` sur chaque ligne (`lib/actions/factures/brouillon-from-events.ts:334`)                                                                                                                                  | reportee | Confirme en verification adversariale. `computeProrataAvoir` (`lib/actions/factures/avoirs.ts:58-223`) n'a aucune branche event-aware : `billedLines` vide -> `loadTotalCommissionContrat` = 0 -> `computeProrataRupture` = `max(0, min(0, 0-0))` = 0. Le repli « remboursement integral » (`avoirs.ts:171-182`) n'est atteint que si `date_debut` ou `duree_mois` manque, donc pas ici. `avoir-dialog.tsx:119-121` ecrase inconditionnellement le champ pre-rempli par `0.00`, sous la legende « Pro-rata temporis calcule automatiquement ». Le pipeline, lui, est protege (`ajustements.ts:268`, `if (billedLines.length > 0)`). Nuance : le 0,00 n'est pas soumettable (`avoir-dialog.tsx:132` et zod `.positive()`), l'impact est un ecran qui affirme faussement qu'il n'y a rien a rembourser, pas un avoir a 0 EUR emis. Non corrige en PR automatique : touche une formule de facturation.                                                                                      |
| Le « Produit » du bandeau Finance projet et le tableau cense le decomposer divergent sur un contrat a `duree_mois` NULL : `fractionRealisee` (`lib/queries/production.ts:78-79`) rend **1** et compte le NPEC plein, `montantProduitOpcoContrat` (`lib/queries/projet-finance-detail.ts:214-215`) rend **0**                                                                                                                                           | reportee | Confirme en verification adversariale. `npecRealise` (`lib/queries/projets.ts:404-408`) est le SEUL consommateur de production du depot sans la garde `!c.date_debut \|\| !c.duree_mois` presente partout ailleurs (`production.ts:248`, `production-rollup.ts:156`, `dashboard/charts.ts:76`, `dashboard/financials.ts:316`, `evolution.ts:104`, `echeancier-dues.ts:166`). Les deux valeurs sont dans le MEME objet (`projet-finance-detail.ts:451-456`) et l'UI affiche `Total : {flux.produit}` juste au-dessus du tableau des lignes (`projet-finance-flux.tsx:78-80,245-255`). `duree_mois` est nullable sans DEFAULT ni trigger (`00006_contrats_eduvia.sql:14`) et la sync ecrit NPEC et duree independamment (`lib/eduvia/sync.ts:672-675,710,717`). `__tests__/production-rollup.test.ts:178-195` pinne « contrats invalides ignores » pour exactement cette forme, regle que `npecRealise` ne respecte pas. Non corrige en PR automatique : touche une formule de production. |
| Le total du pied de page de l'onglet Manuel de `/facturation` ignore le filtre OPCO, alors que le brouillon reellement cree l'applique (`components/facturation/manuel-tab.tsx:227-239` contre `:130-144` et `lib/actions/factures/brouillon-from-events.ts:152-166`)                                                                                                                                                                                  | reportee | Confirme en verification adversariale. `totals` itere sur `projet.events` avec pour seul critere `selected`, et `selected` n'est jamais purge quand le filtre OPCO change (le fichier n'importe meme pas `useEffect`). Deux OPCO sur un projet, AKTO 3000 TTC et OCAPIAT 2000 TTC : « Tout selectionner » puis decocher OCAPIAT laisse le pied de page a 5000,00 TTC alors que le brouillon cree vaut 3000,00, sans mention des 2000,00 ecartes dans le toast. Non corrige en PR automatique : le chiffre de controle affiche avant creation d'une facture est un montant, exclu par le prompt. Attenuation : le brouillon reste relisible en `a_emettre`.                                                                                                                                                                                                                                                                                                                               |
| La carte Finance de la synthese projet libelle « OPCO : X en retard de reglement » un montant qui cumule un retard de NOTRE cote et un retard de l'OPCO (`app/(dashboard)/projets/[ref]/page.tsx:66-68`, `lib/projets/synthese.ts:129`)                                                                                                                                                                                                                | corrigee | Confirme en verification adversariale. `retardFacturation` (`lib/projets/finance-flux.ts:81-94`) compte les jalons `invoiceState == null` des 1 jour de retard, dont le commentaire dit lui-meme « rien a reprocher » a l'OPCO ; `retardEncaissement` (`:96-119`) exige au contraire un jalon transmis et non regle. Un projet dont rien n'a ete transmis affiche donc en synthese « OPCO : 12 000,00 EUR en retard de reglement » et, a un clic, « Retard encaissement 0,00 » sur `/projets/[ref]/finance`. Le cumul est documente comme volontaire (`synthese.ts:46-51`), c'est le libelle qui designait a tort l'OPCO. Corrige par cette PR : libelle ramene a « en retard », sans responsable, aucun montant modifie.                                                                                                                                                                                                                                                                |

## Constats ecartes en verification adversariale

| Constat                                                                                                                                                                                                                                                           | Decision | Raison                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Le CRON snapshot (`app/api/cron/snapshot/route.ts:408-421,474-500`) n'ecoute pas l'`error` de ses selects `projets` et `cdps` : un echec partiel passerait le garde-fou `scopedRows.length > 0` et le DELETE effacerait definitivement les KPI par projet du mois | wontfix  | Le code cite est exact, la chaine d'impact fausse sur trois maillons. (1) Rien a effacer : `mois = startOfMonth(new Date())` et le cron tourne le 1er a 03h (`vercel.json`, `0 3 1 * *`), donc la cle visee est le mois qui COMMENCE ; aucune ligne `scope='projet'` n'existe encore pour cette cle, le DELETE est un no-op. Seule cette route ecrit du scope projet/cdp (`scripts/backfill-kpi-snapshots.ts` ne fait que `global`). (2) Perte non definitive : `computeKpisForScope` ne filtre sur aucune date, le `mois` n'est qu'une etiquette, et un GET avec `CRON_SECRET` regenere un snapshot equivalent n'importe quand dans les 30 jours. (3) Non silencieux et sans lecteur : `counts.projet` est renvoye dans le JSON et logue, et le scope `projet` est aujourd'hui write-only (`lib/queries/kpi-history.ts:24,65` et `lib/queries/dashboard/charts.ts:23` ne lisent que `global` et `cdp` depuis `/pilotage`). Reste vrai et non tranche, sous une autre formulation : les memes `error` ignores permettent, sur echec des queries internes, d'ecrire des KPI GLOBAUX a zero (DELETE+INSERT `:439-459` sans garde sur le contenu), et ce scope-la est reellement lu. |
| La carte Lancement de la synthese ne peut jamais signaler un retard alors que `/projets/[ref]/lancement` affiche des badges « En retard » livres dans le meme commit 849b7a7                                                                                      | wontfix  | Trois erreurs. (1) Chronologie : 849b7a7 est un squash de PR empilees lot 0 a lot 7, et le sous-commit `feat(projet): fonction pure buildSyntheseCards` (lot 0) precede `feat(projet): alertes en retard et enlise sur les etapes` (lot 1) ; le plan du lot 1 dit explicitement s'empiler sur la PR #140. Le commentaire n'etait pas perime a sa naissance. (2) Ce n'est pas un bug : « 2/7 etapes terminees » est exact, aucun resultat faux n'est produit. Le neutre est une decision documentee (`synthese.ts:3-7,63-67`, sous-commit « ne colorer que les cartes dont l'anomalie est calculable ») verrouillee par `__tests__/projet-synthese.test.ts:56-75`, et le lot 1 a statue en laissant la carte telle quelle. (3) « Seul ecran qui masque une alerte » est faux : la carte Contrats est elle aussi codee en dur `ton: 'neutre'`, et la carte pointe a un clic vers l'ecran qui porte les badges. Reste vrai : le commentaire `synthese.ts:84-87` annonce une coloration « au lot 1 » qui n'est pas venue, c'est un commentaire a rafraichir, pas un defaut.                                                                                                           |

| `getProjetPerformance` (`lib/queries/projet-performance.ts:76-79,206-215`) lit `eduvia_invoice_steps` en pleine table, sans `.in('contrat_id', ...)` ni `.range` ni `.order`, puis filtre en JS : une troncature `max_rows` fabriquerait un pourcentage de financement faux, non fail-safe contrairement aux troncatures deja ecartees | wontfix | Le fait de code est exact et distinct du constat du 2026-08-10 sur le meme fichier (colonnes de cout RH), mais le cadrage ne tient sur aucun des trois points. (1) Le seuil n'est pas atteint et le constat le demontre lui-meme : ~100 contrats au repere du depot (`20260805120000_production_post_rupture.sql:32`) x 4 a 8 jalons = 400 a 800 lignes, sous les 1000 de `supabase/config.toml:18`. Le declencheur est un etat futur, exactement le motif qui a valu `wontfix` aux troncatures des 2026-08-10 et 2026-08-11. (2) Le faux 100 % est contradictoire : `totalAmount` et `paidAmount` sortent du MEME jeu tronque (`:211-215`), le volet affiche donc un ratio sur sous-echantillon ; atteindre 100 % exigerait un ordre physique adverse que le constat qualifie lui-meme de non deterministe. (3) La degradation dominante EST fail-safe : `totalAmount <= 0` tombe sur `NULL_VOLET` (`:218-221`), soit « - / Donnees insuffisantes », le volet declare son insuffisance au lieu d'affirmer un chiffre. Portee reduite de surcroit : `financement` n'est rendu que sur `/projets/[ref]/production`, la synthese ne garde que `pedagogie.value`. A rouvrir des que `select count(*) from eduvia_invoice_steps` approche 1000. Restent vrais et non tranches : `invoiceStepsRes.error` n'est jamais teste contrairement a `contratsRes.error` (`:102-107`), et la lecture pleine table a chaque rendu de fiche projet est une incoherence de forme avec toutes ses soeurs. |

## Constats ecartes avant verification, comme deja connus

| Constat                                                                                                                                                      | Raison                                                                                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Supprimer une regle d'archivage remet ses contrats en production a la sync suivante (`ON DELETE SET NULL` sur `contrats.archive_regle_id`)                   | Deja remonte par le monitoring du 2026-08-14, fragment `journal/2026-08-14-monitoring.md`, porte par la PR #159.                                                                                                                                                                                                  |
| Le garde-fou d'idempotence des crons `a-facturer-notif` / `factures-retard` / `echeancier-retard` cede en silence (troncature `max_rows` et `error` non lue) | Les deux moities sont deja tranchees dans `decisions.md` au 2026-08-10 : la troncature en `wontfix` (seuil hors d'atteinte, il faudrait ~333 contrats dus a 3 destinataires pour 1000 lignes, soit plus que le total des contrats en base), et l'`error` non verifiee explicitement notee « remonte separement ». |
