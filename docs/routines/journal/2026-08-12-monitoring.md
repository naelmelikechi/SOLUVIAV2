# 2026-08-12 - Monitoring quotidien

Rapport complet : commentaire du 2026-08-12 sur l'issue #143. PR de l'execution : celle-ci.

Diagnostic vert sur `main` (`8200794`) : `lint`, `typecheck` et `test` (1614 tests,
1 ignore) passent. `npm audit --omit=dev` : les memes avis `high` que les jours
precedents, deja portes par le rapport du 2026-08-08 sur #143, non re-remontes.

Un seul constat candidat retenu apres lecture des fichiers touches par les 30
derniers commits, soumis a deux sous-agents charges de le REFUTER avec consigne de
conclure REFUTE en cas de doute. Les deux ont conclu CONFIRME, sur des angles
differents (existence d'un autre chemin d'edition / portee reelle a l'ecran).

## Constat confirme, remonte sur l'issue #143

| Constat                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Decision | Raison                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Les deux seuils introduits par les lots recents sont annonces reglables depuis `/admin/parametres` alors qu'aucun champ ne les expose : `facturation.delai_reglement_opco_jours` (migration `20260807080000`, lu par `getDelaiReglementOpcoJours`, `lib/queries/parametres.ts:246-254`) est bien charge par la page mais n'a aucun `<Input>` dans `components/admin/parametres-form.tsx:197-264` ; `lancement.seuil_enlisement_jours` (migration `20260806110000`, lu par `getSeuilEnlisementJours`, `:227-232`) porte la categorie `lancement`, que `app/(dashboard)/admin/parametres/page.tsx:26-46` ne charge meme pas. Les commentaires des deux migrations et des deux lecteurs affirment pourtant « modifiable dans /admin/parametres sans redeploiement » | reportee | Confirme par deux verifications adversariales independantes. Les deux seuils pilotent un affichage utilisateur reel : `retardEncaissement` du flux OPCO (`lib/queries/projet-finance-detail.ts:339,441` puis `components/projets/projet-finance-flux.tsx:173-174`) et le badge d'enlisement de la timeline (`app/(dashboard)/projets/[ref]/lancement/page.tsx:29-51`, `lib/lancement/alertes.ts:49`, `components/projets/lancement-etape-item.tsx:181-189`). Aucun autre chemin d'edition de la table `parametres` n'existe : `parametres-form.tsx` est le seul ecrivain (`from('parametres').update`), les sous-pages `/admin/parametres/*` operent sur d'autres tables, et aucun editeur cle/valeur generique n'existe. Aucun runbook ne documente d'UPDATE SQL en prod, et PG prod n'est pas expose (CLAUDE.md) : la seule voie reelle est une migration plus un deploiement, exactement ce que la promesse excluait. Non corrige en PR automatique : ajouter un champ d'administration qui change une valeur vue par l'utilisateur n'entre pas dans la classe des corrections mineures du prompt de la routine. Porte par le commentaire du 2026-08-12 sur #143. |

## Constats ecartes avant verification adversariale

Ecartes par lecture directe, sans mobiliser de sous-agent : le scenario d'echec
ne tenait pas.

| Constat examine                                                                                                                                                                                                                                 | Pourquoi ecarte                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `contratsAArchiver` (`lib/contrats/archivage.ts:47-51`) indexe les regles par `etat_source` dans une `Map` : deux regles actives sur le meme etat, et l'une ecraserait l'autre en silence                                                       | Impossible en base : `contrats_regles_archivage` porte `UNIQUE (etat_source)` (migration `20260807070000`, ligne 60), et `upsertRegleArchivage` traduit deja le `23505` en message lisible (`lib/actions/contrats-regles.ts:76-81`).                                                                                                                                                                                                                               |
| Le garde-fou facturation du cron d'archivage (`app/api/cron/contrats-archivage/route.ts:106-110`) lit `facture_lignes` sans pagination : une troncature `max_rows` ferait archiver un contrat deja facture, cette fois dans le sens fail-unsafe | Le seuil est hors d'atteinte. La lecture est bornee aux seuls candidats, par lots de 200 (`LOT_IDS`), et les candidats sont par construction des contrats restes dans un etat sans issue - ceux-la meme qui n'ont normalement aucune ligne de facture. Il faudrait plus de 1000 lignes de facture rattachees a 200 contrats candidats sur une base d'environ 100 contrats (migration `20260805120000`). A rouvrir seulement si la base change d'ordre de grandeur. |
| `alerteEtape` (`lib/lancement/alertes.ts:60`) signalerait « en retard » une etape pourtant deposee, si `date_realisation` etait absente                                                                                                         | Etat non atteignable : le trigger `set_lancement_date_realisation` pose la date a chaque ecriture en `depose`/`lance`, et la migration `20260806110000` backfille les lignes anterieures depuis `updated_at` (lignes 64-67).                                                                                                                                                                                                                                       |
| `desarchiverContrat` (`lib/actions/contrats-regles.ts:114`) remet un contrat dans le chiffre de la production alors que la doc le dit « reserve aux admins » : un CDP pourrait appeler la Server Action directement                             | Faux : `checkAuth()` (`lib/auth/guards.ts:101-113`) verifie bien le role via `isAdmin`, en plus de `actif`. Le garde est au bon endroit.                                                                                                                                                                                                                                                                                                                           |
| La ventilation de TVA par taux du PDF (`lib/utils/tva-ventilation.ts`) arrondit ligne par ligne cote TypeScript : la somme des lignes de TVA imprimees pourrait ne pas retomber sur le `montant_ttc` d'en-tete calcule en base                  | Les deux formules sont identiques au centime : `floor(montant_ht * taux + 0.5) / 100` cote trigger (`recompute_facture_totaux`, migration `20260630141000`) et `Math.round(montant_ht * taux / 100 * 100) / 100` cote TS, meme sens d'arrondi y compris sur les montants negatifs des avoirs.                                                                                                                                                                      |
