# 2026-08-14 - Monitoring quotidien

Rapport complet : commentaire du 2026-08-14 sur l'issue #143. PR de l'execution : celle-ci.

Diagnostic vert sur `main` (`8200794`, inchange depuis le 2026-08-11, quatrieme
execution consecutive sur le meme commit) : `lint` propre (le warning
`facturation-page-client.tsx:238` note le 2026-08-13 a disparu), `typecheck`
propre, `test` vert (1614 tests, 1 ignore). `npm audit --omit=dev` : les memes 6
avis `high` que les jours precedents (`next` -> `postcss` + `sharp`, `nanoid`,
`fast-uri`, `brace-expansion`), deja portes par le rapport du 2026-08-08 sur
#143, non re-remontes.

`main` n'ayant pas bouge depuis quatre executions, la recherche a porte sur les
zones que les rapports des 2026-08-11, 2026-08-12 et 2026-08-13 n'avaient pas
couvertes : l'archivage automatique des contrats (lot 2), le journal des mails
(lot 6), les agregats de production, et la coherence de la documentation
d'exploitation.

Quatre constats candidats, chacun soumis a un sous-agent charge de le REFUTER
avec consigne de conclure REFUTE en cas de doute. Deux confirmes, deux refutes.

## Constats confirmes

| Constat                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Decision | Raison                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Supprimer une regle depuis `/admin/parametres/archivage-contrats` remet les contrats qu'elle avait archives dans la production, au prochain passage de la sync Eduvia, alors que la modale promet l'inverse. `contrats.archive_regle_id` est declaree `ON DELETE SET NULL` (`supabase/migrations/20260807070000_contrats_archivage_auto.sql:89`) ; le trigger `protege_archivage_auto_contrat` (`20260807110000_correctifs_revue_securite_projet.sql:45-51`) ne re-force `archive := true` que si `OLD.archive_regle_id IS NOT NULL` ; la sync ecrit `archive: false` sur chaque contrat encore renvoye par Eduvia (`lib/eduvia/sync.ts:719`) | reportee | Confirme en verification adversariale, avec reproduction de la chaine complete sur un PostgreSQL 16 portant le schema et les triggers des migrations : apres suppression de la regle puis une sync, le contrat retombe a `archive = false`. Le cron d'archivage ne rattrape jamais, la regle n'existant plus. Non corrige en PR automatique : le correctif touche une contrainte de cle etrangere et un trigger, donc une migration, exclue par le prompt de la routine, et le choix entre `ON DELETE RESTRICT`, une colonne `archive_par_regle` independante de la FK et un simple realignement du texte de la modale demande un arbitrage. Porte par le commentaire du 2026-08-14 sur #143. |
| `docs/CRONS.md` documente deux crons supprimes a la Phase 2 CRM (`prospect-alertes`, `kpi-digest`) et omet huit crons reellement planifies (`echeancier-retard`, `contrats-archivage`, `passation-echeances`, `crm-recap`, `crm-alertes`, `a-facturer-notif`, `process-sync`, `gmail-collecte`) : 18 routes documentees pour 24 planifiees                                                                                                                                                                                                                                                                                                    | corrigee | Confirme en verification adversariale (listes exactes etablies par difference symetrique entre `vercel.json`, `app/api/cron/` et le tableau). Le fichier ne porte aucune reserve d'exhaustivite et se declare normatif (« Ajouter un nouveau cron : 3. Documenter ici »), aucune autre source de verite ne le remplace, et les renvois de `README.md` et `docs/DEPLOYMENT.md` annoncaient 10 et 11 jobs. Corrige dans cette PR.                                                                                                                                                                                                                                                               |

## Constats ecartes en verification adversariale

| Constat                                                                                                                                                                                                                                                                                                                         | Decision | Raison                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `void journaliserEmail(...)` (`lib/email/_send.ts:133`) lance l'ecriture du journal des mails sans `await` ni `waitUntil` : l'invocation serverless pourrait etre gelee avant que l'INSERT dans `emails_envoyes` n'aboutisse, et le bloc Mails de la fiche projet afficherait un historique incomplet de facon non deterministe | wontfix  | L'affirmation porteuse est fausse : `sendEmail` n'est la derniere operation asynchrone d'AUCUN appelant renseignant `projetId`/`clientId`. L'emission de facture enveloppe deja tout `sendEmailForFacture` dans `waitUntil()` de `@vercel/functions` (`lib/actions/factures/emission.ts:185-193`) ; les autres chemins enchainent systematiquement un aller-retour Supabase attendu vers le meme hote apres l'appel (`lib/email/client.ts:209-226` pour `email_envoye`/`email_erreur`, `lib/actions/email.ts:117-136` et `:167-184`, `app/api/cron/devis-relance/route.ts:61-66`), et le chemin devis passe par `logAudit`, qui utilise `after()` de `next/server` (`lib/utils/audit.ts:80`). La requete d'INSERT est de toute facon dispatchee dans le meme tick, avant le `return` de `sendEmail`. Accessoirement, la chaine UI citee etait fausse : `projet-emails-liste.tsx` est un composant client presentationnel, l'appelant reel est `projet-suivi-panel.tsx:29`. Reste vrai et non tranche : envelopper l'appel dans `after()` comme le fait `audit.ts` serait un durcissement coherent avec la convention du depot.   |
| `getEcheancierDuesWith` (`lib/queries/echeancier-dues.ts:466-480`) retourne `dues: []` quand `loadBilledByContrat` echoue : une panne de lecture est indistinguable d'un portefeuille a jour, et le cron `echeancier-retard` repond `success: true` sans alerte, meme classe de defaut que celle corrigee par #148              | wontfix  | Trois affirmations porteuses fausses. (1) Ce n'est pas une regression : la meme sortie `{ hasEcheancierProjets: true, dues: [], upcoming: [] }` existait deja mot pour mot sur l'erreur de lecture `contrats` (`:459-464`) avant le commit, qui n'a fait qu'aligner la troisieme lecture sur les deux autres. (2) L'analogie avec #148 ne tient pas : `app/api/cron/echeancier-retard/route.ts` n'a ni email ni `withEmailLock`, il cree des notifications in-app dedupliquees par le champ `lien` ; un run rate n'empoisonne aucune cle, le lundi suivant recree la notification. La perte est bornee a 7 jours, pas definitive. (3) L'incident n'est pas invisible : `logger.error('queries.echeancier-dues', ...)` (`:539-541`) part en Sentry via `lib/utils/logger.ts:104-133`. Et le changement a strictement ameliore la situation : l'ancien `continue` sur-proposait des jalons deja factures, donc un risque de double facturation sur une numerotation gapless. Reste vrai et non tranche : propager un indicateur d'erreur pour que le cron reponde 500, sur les trois sorties du fichier et pas seulement celle-ci. |

## Correction de cette PR

`docs/CRONS.md` : retrait des deux crons fantomes (ligne de tableau et section
de detail), ajout des huit crons manquants avec leur `schedule` lu dans
`vercel.json` et leur `maxDuration` lu dans chaque `route.ts`. Mention de
`welcome-emails-broadcast`, present sur disque mais volontairement non planifie.
Les comptes perimes de `README.md` (10 et 11 jobs) et `docs/DEPLOYMENT.md`
(11 jobs) sont alignes sur 22 routes `/api/cron/*` et 24 entrees `crons` au
total. Aucune section « Detail par cron » n'a ete redigee pour les huit crons
ajoutes : le tableau est l'index d'exploitation, rediger huit fiches de detail
sortirait de la classe des corrections mecaniques verifiables.

## Rappel : constats deja remontes, toujours ouverts

Verifies encore presents sur `8200794`, non re-remontes :

- Le « Retard facturation » de la commission affiche du retard OPCO en euros
  OPCO (`lib/queries/projet-finance-detail.ts:130-194`) - rapport du 2026-08-13,
  PR de correction de libelles #158 encore ouverte.
- Deux seuils annonces reglables dans `/admin/parametres` sans y avoir de champ -
  rapport du 2026-08-12, PR de memoire #157 encore ouverte.
- Retard d'encaissement commission sans deduction des avoirs ni des paiements -
  rapport du 2026-08-08.
- Les 6 avis `high` de `npm audit --omit=dev` - rapport du 2026-08-08.
