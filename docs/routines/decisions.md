# Decisions sur les constats des routines

Memoire partagee des routines cloud de ce depot. Elles la lisent AVANT de
remonter quoi que ce soit, et n'y ajoutent que des decisions deja prises.

Sans ce fichier, un constat ecarte revient a chaque execution : la seule
deduplication dont dispose une routine est "existe-t-il une PR ou une issue
ouverte ?", et cette question oublie tout ce qui a ete referme. Un constat
juge non pertinent en aout serait donc re-remonte a l'identique en septembre,
indefiniment.

## Format

Une ligne par decision, la plus recente en haut. Cite le fichier concerne
quand le constat est localise, sinon decris-le assez precisement pour qu'il
soit reconnaissable.

Valeurs de la colonne Decision :

- `wontfix` : ce n'est pas un bug ici, et ca ne le deviendra pas.
- `acceptee` : c'est un choix assume, documente par la raison.
- `corrigee` : traitee, ne pas re-remonter.
- `reportee` : reelle mais pas maintenant. Cite l'issue qui la porte.

| Date       | Constat                                                                                                                                                                                                                                                                                                                  | Decision | Raison                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-11 | La requete Gmail de `app/api/cron/gmail-collecte/route.ts:128` concatene TOUS les contacts clients sans borne (`(from:X OR to:X)` joints par `OR`) : au-dela de quelques dizaines de contacts elle serait rejetee, `listMessageIds` retournerait `[]` en silence et la collecte resterait figee a zero sans signal rouge | wontfix  | Le mecanisme de degradation silencieuse est reel, mais le declencheur est faux d'un ordre de grandeur. Limite de transport mesuree empiriquement entre 16 et 17 Ko d'URL (401 jusqu'a 16000 caracteres, 400 a partir de 17000), soit environ 200 contacts a ~80 caracteres encodes par terme, contre « quelques dizaines » annonce. Or `client_contacts` n'est alimentee que par saisie manuelle admin (`lib/actions/clients/contacts.ts`, `addClientContact`), jamais par la sync Eduvia, sur une base d'environ 100 contrats (migration `20260805120000`). Et le cron est inerte : `GMAIL_COLLECTE_ACTIVE=false` dans `.env.example`, sortie ligne 52 avant tout appel reseau, activation conditionnee a deux prerequis non techniques documentes dans `docs/runbooks/gmail-collecte.md`. A rouvrir seulement si `client_contacts` depasse reellement 200 lignes. Reste vrai et non tranche : `listMessageIds` degrade une reponse non-OK en `[]` avec un simple `logger.warn`, aucune supervision ne lit ce cron (absent de `/admin/syncs` et de `docs/CRONS.md`), et `maxResults = 50` sans pagination plafonne de toute facon la collecte a 50 messages par CDP et par jour, borne bien plus proche que la longueur de la requete. |
| 2026-08-11 | Le `select` sur `contrats` de `app/api/cron/contrats-archivage/route.ts:40-43` n'est ni pagine ni passe par `fetchAllRows`, contrairement a la lecture `facture_lignes` corrigee juste a cote : il est expose a la troncature silencieuse `max_rows` de PostgREST                                                        | wontfix  | Le fait de code est exact et le plafond de 1000 est bien reel (`supabase/config.toml:18`, `lib/supabase/fetch-all.ts`), mais il vaut environ 10 fois la base entiere (« 4 contrats rompus sur ~100 », migration `20260805120000` ; plus gros tenant reel aux ids Eduvia 1 a 58). Surtout, la troncature serait ici strictement fail-safe, a l'exact inverse du cas `facture_lignes` : `decisions` est inclus dans `candidats`, lui-meme inclus dans les contrats charges, donc elle ne peut qu'archiver MOINS, jamais a tort. La troncature de `facture_lignes` amputait la liste des INTERDICTIONS (un contrat deja facture serait sorti de la production, sur une numerotation gapless) ; celle-ci ampute la liste des candidats. Aucune perte definitive non plus : la regle `jours > delai_jours` est monotone dans le temps (`contract_state_changed_at` pose par trigger), et le vivier `archive = false` draine a chaque archivage reussi. Meme motif que la ligne `buildContratIdMap` du 2026-08-07, deja acceptee sur des arguments plus faibles.                                                                                                                                                                              |
| 2026-08-07 | `buildContratIdMap` (`lib/eduvia/sync.ts`) lit `contrats` sans pagination ni chunk, contrairement a sa soeur `snapshotExistingContrats` : progressions et jalons de facturation seraient perdus en silence au-dela de 1000 contrats pour un seul client                                                                  | acceptee | Le fait de code est exact, ses consequences non. Le seuil (plus de 1000 contrats pour UN client) vaut environ 10 fois la base entiere (« 4 contrats rompus sur ~100 », migration `20260805120000`) et 20 fois le plus gros tenant reel (ids Eduvia 1 a 58). Contrairement a `snapshotExistingContrats`, l'erreur EST correctement remontee ici (`result.errors` + abandon du client), et les donnees concernees sont de purs miroirs re-upsertes a chaque run, donc aucune perte definitive. A rouvrir seulement si un client depasse reellement 1000 contrats.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-08-07 | Alertes de saturation CDP (`app/api/cron/passation-echeances/route.ts`) : le palier `cdp_seuil_alerte` est persiste avant l'envoi, donc un echec d'envoi abandonnerait la boucle et perdrait l'alerte DEFINITIVEMENT                                                                                                     | wontfix  | Les deux affirmations porteuses sont fausses. `sendCdpSaturationEmail` ne leve jamais (toutes les branches d'echec de `lib/email/_send.ts` retournent `{ success: false }`), donc la boucle va toujours a son terme et les CDP suivants sont traites. Et `computeChargeAlertes` persiste aussi les REDESCENTES (`__tests__/charge-alertes.test.ts`, « redescente : pas de notification mais re-armement persiste »), donc la perte dure jusqu'au prochain franchissement descendant, pas eternellement. Ne pas re-remonter sous cette formulation. Reste vrai, et non tranche ici : le retour de `sendCdpSaturationEmail` n'est pas teste (ligne ~263), un echec Resend reste silencieux.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

<!-- Ajoute tes lignes au-dessus de ce commentaire, sous l'en-tete du tableau. -->
