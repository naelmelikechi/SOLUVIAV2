# 2026-08-17 - Audit hebdomadaire profond

Rapport complet : issue GitHub « Audit hebdomadaire - semaine du 2026-08-17 ».
PR de l'execution : celle-ci.

Diagnostic vert sur `main` (`8200794`) : `lint`, `typecheck` et `test` (186 fichiers,
1614 tests, 1 ignore) passent. `npm audit --omit=dev` : les memes 6 avis `high`
que les jours precedents, deja portes par le rapport du 2026-08-08 sur #143,
non re-remontes.

Methode : 8 sous-agents de dimension en parallele, 43 constats candidats, puis
un sous-agent verificateur par constat (ou par lot thematique) charge de le
REFUTER avec consigne de conclure REFUTE en cas de doute. **20 constats ecartes
en verification adversariale**, dont plusieurs pour des raisons durables
consignees ci-dessous.

## Constats ecartes en verification adversariale

| Constat                                                                                                                                                                                                                                                                                                            | Decision                             | Raison                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| L'acceptation d'un devis par le portail public ne conserve aucune trace technique : `accept_devis_public` ecrit `acceptation_ip` et `acceptation_user_agent` (`supabase/migrations/20260523100200_devis_public_rpcs.sql:205-212`) mais `app/devis/public/[token]/actions.ts:37-42` ne passe que 3 des 5 parametres | wontfix sous cette formulation       | Le cablage manquant est reel (3 arguments passes, verifie), mais le titre et la gravite sont faux. La trace IP + user-agent EXISTE : `get_devis_public` insere une ligne dans `devis_public_views` avec ip et user-agent a chaque consultation (`:143-144`), et le formulaire d'acceptation n'est atteignable QUE par cette page. Ce qui manque n'est pas la donnee mais son rattachement a l'evenement d'acceptation. Le cadrage juridique (« engageante », « preuve », « contestation ») est une aggravation non etayee (regle 2) : `statut = 'accepte'` ne cree ni projet, ni contrat, ni facture — il debloque une action interne authentifiee et auditee (`lib/actions/devis-to-facture.ts:50`, `:195`) — et `signature_requests` n'a aucun `devis_id`, le circuit Yousign etant sans point de contact avec le portail devis. Le jeton n'est pas devinable (`gen_random_uuid()`, 122 bits, `:28`). Reste vrai et non tranche, en mineur : les deux parametres sont a deux lignes de l'appel (`actions.ts:22` calcule deja l'IP pour le rate limit), et `check_devis_transition` (`:53-56`) n'autorise aucune sortie de l'etat `accepte`, donc une acceptation erronee est irreversible par l'application. Piege du correctif : `clientIpFromHeaders` renvoie la chaine `'unknown'` a defaut d'en-tete, valeur qui ferait echouer le cast `INET`.                                                                                                                                                                                                                                                            |
| Les 9 policies `crm_pipeline_all` exposent un `DELETE` de `crm.comptes` qui efface en cascade 8 des 9 tables metier du schema CRM                                                                                                                                                                                  | wontfix                              | Le decompte des cascades est exact mais il mesure la mauvaise chose. Un titulaire de `pipeline_access` peut deja detruire 6 des 9 tables PAR L'INTERFACE : `deleteOpportunite` (`lib/crm/actions/opportunites.ts:204-211`) cascade sur `activites`, `relances`, `rdv` et `rdv_commerciaux`, et `deleteAdresse` (`lib/crm/actions/adresses.ts:53-64`, que le constat avait manquee) couvre `adresses`. Le differentiel reel du DELETE brut est de 2 tables (`comptes`, `contacts`), pas 8. Et la cascade est **documentee a l'utilisateur** : `components/crm/pipeline/opp-detail-body.tsx:305-306` affiche « ses activites, relances et RDV lies seront definitivement supprimes. Action irreversible. » derriere un `ConfirmDialog`. Deux autres affirmations sont fausses (regle 1) : `supabase/tests/30_users_privilege_escalation.sql:298` et `31_role_helpers_actif.sql:75-94` couvrent bien la porte d'entree `has_pipeline_access()`, et `lib/crm/actions/pont.ts:178` appelle bien `logAudit`. Le design `for all` est une decision documentee (`20260807090000_escalade_flags_de_droits.sql:10`), pas un angle mort. Ne pas re-remonter sous cette forme. Reste vrai et non tranche : `crm.etapes` est une table de configuration de 7 lignes qu'aucun code n'ecrit (`lib/crm/queries/opportunites.ts:19-21` : « Aucune action n'ecrit etapes aujourd'hui ») mais que la policy laisse en ecriture ; un `PATCH` sur `etapes.type` ferait declencher le pont vers `public.clients` en `service_role` au geste ordinaire d'un collegue.                                                                   |
| `fetchList` (`lib/eduvia/client.ts:446-461`) lit une seule page sans regarder `meta`, et son resultat pilote un `DELETE` d'orphelins sur `eduvia_invoice_steps`                                                                                                                                                    | wontfix                              | Troisieme constat de la famille « pagination Eduvia », apres deux deja classes `acceptee`, et le plus faible des trois : sa premisse centrale n'est pas etablie et le depot porte deux decisions de conception contraires, prises SPEC EN MAIN. `docs/superpowers/plans/2026-04-17-eduvia-sync-refactor.md:415-419` distingue explicitement `fetchAllPages` / `fetchOne` / `fetchList` d'apres l'OpenAPI, et `docs/superpowers/specs/2026-06-30-sync-hardening-odoo-eduvia.md:302`, chantier entierement consacre a la pagination, re-examine `fetchList` et le classe « mono-requete non pagine, inchange ». L'analogie avec `invoice_lines` ne se transpose pas : un bordereau porte plusieurs LIGNES par step. Defaut logique fatal : meme en supposant la pagination, la consequence ne suit pas. En tri ascendant les steps hors page 1 n'ont jamais ete synchronises, donc ne sont pas en base, donc jamais classes orphelins ; en tri descendant ce sont les plus ANCIENS qui tombent, donc ceux que la garde `facture_lignes` protege — l'inverse de ce qui est annonce. Repere de volumetrie : `docs/superpowers/plans/2026-04-17...:1290-1293` donne 18 steps pour 6 contrats, soit 3 par contrat, contre 25 pour le seuil. Reste vrai et non tranche, en durcissement : `fetchList` ne peut structurellement pas detecter une troncature (l'enveloppe est castee sans validation), la seule garde est binaire (`sync.ts:1144`, `steps.length === 0`), et le garde-fou `orphan_resync_warning` (`app/api/cron/eduvia-audit/route.ts:119-131`) ne couvre que `contrats_archived_orphan`, pas les steps. |
| Une panne de lecture cote push Odoo n'ecrit aucune ligne dans `odoo_sync_logs` (`lib/odoo/sync.ts:51-57`, `:311-317`), et `getOdooSyncHealth` ne gardant que le log le plus recent par couple, un succes posterieur masque les echecs du meme run                                                                  | wontfix                              | Les six faits de code sont exacts, les deux scenarios tombent. La carte annonce exactement ce qu'elle fait : `components/admin/syncs/odoo-health-card.tsx:43-45` affiche en sous-titre « Dernier log par flux (direction / type d'entite) ». L'echec intra-run ECRIT bien sa ligne `error` (`sync.ts:274-280`) : elle est en base, simplement non retenue par une agregation, et `scripts/audit-odoo-reconciliation.ts:302-312` la relit sans reduction. Surtout, la panne n'est pas silencieuse : `logger.error` est forwarde a Sentry (`lib/utils/logger.ts:131-133`, `instrumentation.ts:6-22`), et le depot cite des tickets Sentry reels. Enfin le prejudice restant, formule proprement, est « l'ecran de sante ne signale pas une sync en panne », c'est-a-dire mot pour mot l'angle mort **deja declare** de #122 (« absence d'alerte sur la sante des syncs »). Fenetre de degradation : une heure (cron horaire, re-push idempotent). Reste vrai : les deux chemins `push` sont les seuls des quatre a n'ecrire aucun log de run agrege, la ou `pullPayments` et `pullCancellations` en ecrivent un. Amelioration de proprete, meme classe que `contrats-archivage` le 2026-08-08.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| L'avoir de rupture est calcule a l'echelle du CONTRAT mais plafonne a la facture d'origine : le montant propose serait impossible a emettre, et le CDP sur-crediterait de 159,95 EUR en plafonnant manuellement chaque facture                                                                                     | wontfix                              | Trois des quatre affirmations verifiables sont fausses et la quatrieme est un doublon. L'UI plafonne DEJA : `components/facturation/avoir-dialog.tsx:119` fait `Math.min(result.suggestedAmount ?? 0, montantHtDefault)`, plus un garde client a `:140-143`. Le cul-de-sac decrit n'existe pas, et `createAvoir(319.96)` n'est jamais construit par l'application. L'affichage allegue n'existe pas non plus : `avoir-dialog.tsx:285-300` ne rend jamais `montantLigneHt`, la « contradiction visuelle sur la meme ligne » porte sur du code non lu (regle 1). Le chiffre est faux (319,91 mesure, pas 319,96 ; trop-percu 160,00 et non 159,95). Ce qui reste apres retrait est integralement le constat 4 de l'audit #151 (« l'avoir de rupture ne deduit jamais les avoirs deja emis »). Reste vrai, non tranche et a REECRIRE de bout en bout si on le rouvre : le desalignement produit l'inverse de ce qui etait allegue, une troncature silencieuse A LA BAISSE — `resolveAjustement` (`lib/actions/echeanciers.ts:296-323`) accepte de clore un ajustement de -159,91 avec un avoir de 79,97, sans jamais controler le montant.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| La sortie anticipee `IF NEW.ref IS NOT NULL THEN RETURN NEW` de `generate_facture_ref` court-circuite le compteur gapless : un INSERT direct avec `ref` et `numero_seq` creerait des centaines de trous irreversibles                                                                                              | wontfix                              | Le mecanisme SQL est exact (ordre des tests verifie ligne a ligne) mais c'est une porte de sortie DELIBEREE, presente depuis le schema initial et utilisee par le depot lui-meme : `supabase/tests/02_rls_facture_delete.sql:63-72` s'en sert avec le commentaire « bypass le trigger qui exige a_emettre -> emise via UPDATE ». La supprimer casserait ce fixture et interdirait toute reprise d'historique. Aucun chemin applicatif n'y accede : les cinq seuls INSERT sur `factures` (`brouillon-from-events.ts:287`, `avoirs.ts:384`, `brouillons-shared.ts:101`, `brouillon-echeancier.ts:176`, `devis-to-facture.ts:144`) posent tous `statut: 'a_emettre'` et ne renseignent ni `ref` ni `numero_seq` : ils sortent a l'etape (1), pas (2). Le modele de menace ne tient pas : le meme admin peut deja inserer `statut:'emise'` SANS `ref` et obtenir une fausse facture regulierement numerotee. Reste vrai et non tranche : aucun garde-fou ne detecte a posteriori un trou dans la serie, les index uniques n'imposant que l'unicite.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Facturation « TTC inclus » a taux 0 : pour un client UE non-FR, le diviseur vaut 1 et le montant TTC est facture tel quel en HT, soit +20 % de base imposable                                                                                                                                                      | wontfix                              | Le point decisif tranche contre le constat. La convention n'est pas « TTC au taux francais » mais « **Total TTC = montant attendu par le client** », enonce trois fois : `lib/actions/factures/brouillon-from-events.ts:206-212`, `lib/utils/reste-a-facturer.ts:138-141` (« coherent avec `computeFactureTotauxTtcInclus` ») et `lib/queries/billable-events/types.ts:142-144` (le regime CLIENT est explicitement la variable de la derivation). A taux 0, `{totalHt: 960, tva: 0, ttc: 960}` respecte cet invariant ; la division `/1,20` proposee le VIOLERAIT et desynchroniserait la facture de l'ecran « reste a facturer ». `lib/utils/convention-commission.ts` ne sert pas ce chemin (il modelise production/RAF/KPI depuis `NPEC x taux`, cf. son en-tete `:13-22`) : cette divergence-la est deja classee `acceptee`. Declencheur inexistant par ailleurs : aucun client UE non-FR dans le depot (`supabase/seed.sql:54` ne porte que le numero FR de SOLUVIA) et HEOL, client francais, est le seul projet en `engagement`. Reste vrai, en durcissement : `resolveTvaRegime` decide de l'autoliquidation sur les 2 premiers caracteres d'un champ en saisie libre non validee (`lib/actions/clients/crud.ts:35`), et `emission.ts:88-99` ne controle que la non-vacuite.                                                                                                                                                                                                                                                                                                                            |
| Les 13 `export const revalidate` du tableau de bord sont inoperants (le layout lit les cookies), ce qui serait la CAUSE RACINE du constat 6 de #151 ; `/pilotage` bloque le rendu sur une cascade Eduvia sans Suspense ; le cron snapshot recharge le referentiel par projet et par CDP                            | wontfix pour les trois formulations  | Trois symptomes d'un seul constat DEJA remonte (constat 6 de #151, « le referentiel Qualiopi est recharge integralement a chaque affichage, sans cache ») : perimetre gonfle par eclatement (regle 3). Le build a ete lance : les 13 segments sortent bien `ƒ Dynamic`, mais un Full Route Cache n'aurait JAMAIS pu les couvrir — ce sont des pages authentifiees filtrees par RLS, une entree partagee serait une fuite. Ces 13 lignes sont mortes, pas trompeuses (aucun commentaire, aucune doc n'affirme que l'equipe croit avoir ce cache). Sur `/pilotage` : `app/(dashboard)/pilotage/loading.tsx` EXISTE et cree la frontiere Suspense, la page ne reste jamais blanche ; la section est derriere un onglet opt-in (`tab === 'ratios'`, defaut `''`) ; et trois autres pages Qualiopi appellent `getReferentiel` sans Suspense, donc « seul appelant qui y echappe » est faux. Le chiffrage (~48 appels par referentiel) derive de la fixture `quality-client-mock.ts`, pas du reel. **Reste vrai, neuf, prouve et remonte separement** : hors rendu React, `cache()` est un pur passe-plat sans memoisation (`react.react-server.production.js:296-300` ; dispatcher affecte uniquement en `react-server-dom-webpack-server.node.production.js:863` ; sonde `node --conditions=react-server` : 3 invocations identiques = 3 executions). Le correctif du constat 6 de #151, s'il passe par `cache()`, ne couvrira donc pas le cron `/api/cron/snapshot`.                                                                                                                                                |
| La sync Eduvia ecrit ses tables de reference ligne par ligne (~250-300 requetes HTTPS sequentielles par client et par run), au risque de depasser `maxDuration = 300` et d'etre tuee avant `logSyncRun`                                                                                                            | wontfix                              | Le motif est reel, le cadrage faux sur quatre points. L'argument des « cinq `oxlint-disable` poses a la main » s'effondre : la regle est neutralisee **53 fois** dans le depot, dont 18 dans `lib/eduvia/sync.ts` seul, y compris sur les `chunk(..., DELETE_CHUNK)` que le constat cite comme exemples de bonne pratique — c'est l'idiome maison pour toute boucle `await`, pas un indice d'intention. Deux des cinq boucles ne sont pas sequentielles : `syncProgressions` (`:945-948`) et `syncInvoiceData` (`:1011-1014`) passent par `mapWithConcurrency(..., 5)`. Le chiffrage est gonfle : ~150-180 requetes pour le plus gros tenant (58 contrats), pas 250-300. Surtout, le mur `maxDuration` est **deja traite** : `SYNC_TIME_BUDGET_MS = 240_000` (`:1444`, teste par `__tests__/eduvia-sync-budget.test.ts:38`) sort proprement de la boucle avec un curseur de rotation anti-famine. Et le scenario final est faux sur ses deux maillons : `logSyncRun` est appelee PAR CLIENT (`:1627-1633`), et `deriveSyncState` bascule en `degraded` apres 65 min (`lib/queries/syncs.ts:84-96`), pas en `ok`. Reste vrai, en mineur : batcher les 4 tables de reference economiserait ~150 allers-retours par run, au prix de l'imputation d'erreur ligne par ligne.                                                                                                                                                                                                                                                                                                                                          |
| L'index `idx_odoo_sync_logs_created_at_desc` a ete droppe par le batch « 53 indexes unused » de mai et oublie par la remediation du 15 juin, sur une table append-only jamais purgee                                                                                                                               | wontfix                              | Aucun scenario d'echec : le constat chiffre lui-meme ~17 500 lignes/an, volume auquel un Seq Scan coute quelques millisecondes, et le concede (« mineur aujourd'hui »). Trois erreurs d'instruction : l'index designe n'est pas le bon (la requete de `lib/odoo/sync.ts:744-753` filtre sur `direction` + `entity_type` + `entity_id`, l'index utile serait composite) ; deux lecteurs plus importants ne sont pas cites (`sync.ts:823-828` et `app/api/webhooks/odoo/move-cancelled/route.ts:124-130`, qui gouvernent la dedup des notifications) ; et l'argument de poids tombe, les deux ecritures inconditionnelles par run portant des payloads minuscules (`:609`, `:936`), le `payload` volumineux n'apparaissant que sur les rares push de facture. Enfin la singularisation est arbitraire : le batch a droppe 53 index et la remediation n'en a repris que 2 ; `odoo_sync_logs` est l'un des 51 non repris, aux cotes de candidats plus chauds. Regle 4 non respectee (aucun etiquetage).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `devis_public_views` accumule une ligne par consultation du portail public, avec IP et user-agent, sans aucun lecteur et sans aucune purge                                                                                                                                                                         | wontfix comme constat de performance | La croissance est **doublement bornee**, et le constat ne l'a pas vu. L'`INSERT` (`supabase/migrations/20260523100200_devis_public_rpcs.sql:143-144`) est place APRES les deux `RAISE EXCEPTION` (`:135-141`) : sans token vivant et non expire, aucune ligne n'est ecrite — il n'existe donc aucun flux anonyme capable d'alimenter la table. Volume metier de l'ordre de la centaine de lignes par an, avec un index deja present (`:109`) et un `ON DELETE CASCADE` (`:102`). Le fail-open du rate limit est de surcroit **alerte** : `lib/utils/rate-limit.ts:88-91` appelle `alertFailOpen('Upstash non configure')`, route vers Sentry, avec le commentaire « le fail-open est un choix assume mais il doit etre VISIBLE ». Depouille de son habillage performance, le constat dit « une table journalise des consultations sans lecteur et sans purge », c'est-a-dire l'angle mort **deja declare** de #122 (« conformite RGPD et purges annoncees mais non implementees »). Reste vrai, en hygiene : ecriture morte (aucun `.ts` du depot ne lit la table) dont la finalite annoncee en commentaire (« utile pour relances ») n'a jamais ete implementee — le cron `devis-relance` travaille exclusivement sur `devis`.                                                                                                                                                                                                                                                                                                                                                                                  |
| Le schema `crm` n'a aucun test pgTAP : la suppression en cascade d'une opportunite, ouverte a tout titulaire de `pipeline_access`, n'est pinnee nulle part                                                                                                                                                         | wontfix                              | Ne remplit pas le critere : le constat ne nomme aucun bug. Il decrit une suppression qui fonctionne comme prevu — l'UI l'expose, la policy l'autorise, la cascade est declaree. Un test verrouillerait une decision de conception, il n'attraperait rien. Le caractere assume n'est pas une inference : `components/crm/pipeline/opp-detail-body.tsx:305-306` decrit la cascade a l'utilisateur, nominativement et avec mention d'irreversibilite, derriere un `ConfirmDialog`. Le compte « 11 modules sur 12 » sur-compte de deux (`errors.ts` est un helper, `insee.ts` un client d'API), et 13 fichiers de test vitest couvrent `lib/crm/` : c'est la couche `actions` qui est nue, pas le CRM. Doublon par ailleurs avec le constat DELETE `crm.comptes` ci-dessus. Reste vrai et a instruire separement, avec son propre scenario : `deleteOpportunite` (`lib/crm/actions/opportunites.ts:204-211`) n'ecrit aucune ligne d'audit, `logAudit` n'apparaissant dans `lib/crm/` que dans `pont.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Le bouton « Supprimer » d'une absence supprime la ligne sans confirmation, seule action destructrice de l'interface sans garde-fou                                                                                                                                                                                 | wontfix                              | Le sur-affirmatif est faux (regle 3) : trois autres suppressions dures partent au clic sans confirmation — `components/devis/devis-lignes-editor.tsx:227` (ligne de devis, donnee commerciale), `components/equipe/team-chat.tsx:693`, et `components/projets/lancement-etape-item.tsx:515`, ce dernier dans un fichier ou la suppression de document juste au-dessus passe, elle, par `ConfirmDialog` (`:406-413`). Ce n'est donc pas une exception mais une classe d'incoherence UI, c'est-a-dire la generalite que le cadrage d'audit interdit. Les deux boutons ne sont pas indifferencies (`variant="outline"` + `text-red-600` contre un primaire plein) et « Supprimer » n'apparait qu'en mode edition. Perte quasi nulle : une ligne recreable en dix secondes dans le meme panneau, sans solde de conges ni registre legal en aval. Aucun scenario d'echec formulable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Le garde-fou anti-drift est structurellement incapable de signaler une migration du depot jamais appliquee en prod, cas deja survenu « sur 124 migrations d'un coup »                                                                                                                                              | wontfix                              | Titre faux et perimetre gonfle d'un facteur ~60. Le script SIGNALE : `scripts/check-schema-drift.ts:283-286` enumere nominativement chaque objet local absent de prod ; il ne bloque pas, ce qui est un choix de severite, pas un aveuglement. Le chiffre « 124 migrations » est dementi par le fichier meme que le constat cite : `20260805191000_repair_documents_storage_policies.sql:4-11` nomme **2** migrations concernees. Surtout, le chemin est deja couvert ailleurs : `scripts/migrate-supavia.ts:347-356` fait `process.exit(1)` a la premiere migration en echec et `.github/workflows/migrate-supavia.yml:38-39` tourne sur tout push `main` touchant `supabase/migrations/**` — une migration qui ne s'applique pas met bien le job au rouge. Le seul residu est le chemin `--baseline`, verrouille par `MIGRATE_BASELINE_CONFIRM` et jamais appele par la CI. Enfin le remede evident est inutilisable : rendre `localOnly` bloquant rendrait rouge par construction toute PR ajoutant une migration (`sql-tests.yml:6-11`). Reste vrai, en amelioration d'une ligne : conditionner le blocage a `github.event_name == 'schedule'`, pour que le run hebdomadaire du lundi, lui, morde.                                                                                                                                                                                                                                                                                                                                                                                                           |
| La dimension `rls` du garde-fou anti-drift exclut `storage` alors que sa dimension `policy` l'inclut : une desactivation de RLS sur `storage.objects` passerait inapercue                                                                                                                                          | wontfix                              | Scenario purement hypothetique, et le constat l'admet lui-meme (« rien ne suggere que RLS soit desactivee »). `20260805191000_repair_documents_storage_policies.sql:4` etablit au contraire, par lecture directe de la prod le 2026-08-05, que `storage.objects` a RLS active. Aucun incident passe, aucune entree nommable produisant un resultat faux : l'entree est « un humain tape un DDL que rien ne le pousse a taper ». Et l'exclusion est plausiblement deliberee : aucune des 223 migrations ne contient d'`ALTER TABLE storage.objects ... ROW LEVEL SECURITY`, ce flag etant pose par le bootstrap de la plateforme — le comparer entre une prod PG 15.8 self-hosted et un local PG 17.6 est exactement la classe de faux positifs que l'en-tete du script (`:50-62`) dit vouloir eviter. La dimension `trigger` inclut d'ailleurs storage : seule `rls` l'exclut.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `supabase/functions/` n'existe pas alors que `CLAUDE.md` annonce des Edge Functions, et aucun pipeline ne saurait en deployer une                                                                                                                                                                                  | wontfix                              | Inexactitude d'une ligne dans une puce de la section `## Tech Stack` de `CLAUDE.md:13`, qui enumere les briques de la plateforme Supabase — quatre des six sont bien utilisees. Le scenario est construit a rebours : il postule qu'un developpeur ecrit une Edge Function PARCE QUE une puce de stack la mentionne, alors qu'aucun plan du depot n'en propose une et que le chemin etabli pour tout traitement asynchrone est unanime et visible (23 routes `app/api/cron/*` declarees dans `vercel.json`, plus `docs/CRONS.md` et `CLAUDE.md:44`). Ni entree, ni etat, ni valeur : pas de scenario d'echec. Correction de deux mots a glisser dans une PR, pas un constat d'audit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Le chip « A facturer » et l'alerte « Echeances pretes » de `/accueil` lisent la table morte `echeances`                                                                                                                                                                                                            | corrigee dans sa formulation         | Erreur de fait decisive : ces deux affichages ne sont **pas** sur `/accueil`. `app/(dashboard)/accueil/page.tsx:24-77` ne rend jamais `ContextChips` ni `AlertsStrip` ; ils sont montes par `dashboard-page-client.tsx`, lui-meme monte par `app/(dashboard)/pilotage/page.tsx:129-133`. Pour un admin, `/accueil` lit `getContratsNonFacturesGlobal()`, source live. Tout l'argument de gravite (« page d'entree quotidienne ») tombe. Le fond est neanmoins exact et re-remonte sous la bonne forme dans le rapport : sur les 10 acces a `echeances`, 7 `select` et 2 `update` de rattachement, aucun `insert`, et aucun trigger ni RPC cote SQL.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

## Note d'hygiene sur l'execution

Trois sous-agents ont laisse des artefacts dans l'arbre de travail pendant
l'audit : une ligne `export const AUDIT_TMP_BAD` dans `lib/utils/temps-totals.ts`
et une bascule de `next.config.ts` (test empirique de `ignoreBuildErrors`,
reintroduits deux fois), et deux repertoires de routes jetables sous
`app/devis/public/` (test empirique des frontieres d'erreur). Tous ont ete
annules avant commit ; `git status` etait vide au moment du commit. A garder en
tete pour les prochaines executions : un sous-agent autorise a tester
empiriquement doit restaurer, et l'orchestrateur doit verifier l'arbre avant de
committer plutot que de s'y fier.
