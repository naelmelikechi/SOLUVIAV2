# Decisions sur les constats des routines

Memoire partagee des routines cloud de ce depot. Elles la lisent AVANT de
remonter quoi que ce soit, et n'y ajoutent que des decisions deja prises.

Sans ce fichier, un constat ecarte revient a chaque execution : la seule
deduplication dont dispose une routine est "existe-t-il une PR ou une issue
ouverte ?", et cette question oublie tout ce qui a ete referme. Un constat
juge non pertinent en aout serait donc re-remonte a l'identique en septembre,
indefiniment.

## Format

Une ligne par decision, la plus recente en haut. Cite le fichier concerne
quand le constat est localise, sinon decris-le assez precisement pour qu'il
soit reconnaissable.

Valeurs de la colonne Decision :

- `wontfix` : ce n'est pas un bug ici, et ca ne le deviendra pas.
- `acceptee` : c'est un choix assume, documente par la raison.
- `corrigee` : traitee, ne pas re-remonter.
- `reportee` : reelle mais pas maintenant. Cite l'issue qui la porte.

| Date       | Constat                                                                                                                                                                                                                                                                                                                                                                                                           | Decision | Raison                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-08-10 | Archiver un template d'echeancier encore assigne a des projets les bascule en silence sur le template par defaut global : `archiveEcheancierTemplate` (`lib/actions/echeanciers.ts:220-251`) ne compte aucun projet referencant l'id, et `resolveProjetEcheancier` (`lib/echeancier/calc.ts:271-284`) retombe sans bruit sur le defaut quand le template assigne est absent de la liste filtree `archive = false` | reportee | Confirme par deux verifications adversariales independantes. L'echeancier de facturation ET la reference de calcul de l'avoir de rupture changent, sans avertissement. Porte par un commentaire de l'issue #143 du 2026-08-10. Non corrige en PR automatique : touche une formule de facturation, exclue par le prompt de la routine.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-08-10 | `getProjetPerformance` (`lib/queries/projet-performance.ts:61`) lit en service_role les 7 colonnes de cout RH revoquees a `authenticated` par `20260428120000_user_cost_fields.sql` et publie leur agregat dans le popover « Rentabilite » de `/projets/[ref]/production`, sans le `assertAdmin()` qui garde `getUserCostInfo`                                                                                    | acceptee | Le fait de code est exact (aucun garde de role : ni la page, ni `app/(dashboard)/projets/[ref]/layout.tsx`, ni `proxy.ts`, ni le composant), mais l'attribution a une personne n'est pas etablie. La population qui peut SAISIR du temps sur un projet est bornee par le meme predicat que celle qui peut le LIRE (`cdp_id` / `backup_cdp_id` / `est_interne`, `lib/queries/temps.ts:127-129`) : dans le cas modal, le seul contributeur est le lecteur lui-meme et le nombre affiche est son propre cout. Et sans cout personnalise en base (aucune valeur en seed, aucune fixture), `resolveEmployeeCost` retombe sur les defauts : le popover ne divulgue alors que 37,04 EUR/h, constante publique du depot (`lib/utils/employee-cost.ts:25-33`, `__tests__/employee-cost.test.ts:53-66`). A ROUVRIR des qu'un cout personnalise existe reellement en base (`select count(*) from users where salaire_brut_annuel is not null`), ou si le volet Rentabilite devient visible d'un role qui ne peut pas saisir sur le projet. Reste vrai et non tranche : `getProjetPerformance` est le seul appelant non-admin du pipeline de cout employe, contrairement a la regle enoncee en `lib/queries/employee-cost.ts:11-15`. |
| 2026-08-07 | `buildContratIdMap` (`lib/eduvia/sync.ts`) lit `contrats` sans pagination ni chunk, contrairement a sa soeur `snapshotExistingContrats` : progressions et jalons de facturation seraient perdus en silence au-dela de 1000 contrats pour un seul client                                                                                                                                                           | acceptee | Le fait de code est exact, ses consequences non. Le seuil (plus de 1000 contrats pour UN client) vaut environ 10 fois la base entiere (« 4 contrats rompus sur ~100 », migration `20260805120000`) et 20 fois le plus gros tenant reel (ids Eduvia 1 a 58). Contrairement a `snapshotExistingContrats`, l'erreur EST correctement remontee ici (`result.errors` + abandon du client), et les donnees concernees sont de purs miroirs re-upsertes a chaque run, donc aucune perte definitive. A rouvrir seulement si un client depasse reellement 1000 contrats.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-08-07 | Alertes de saturation CDP (`app/api/cron/passation-echeances/route.ts`) : le palier `cdp_seuil_alerte` est persiste avant l'envoi, donc un echec d'envoi abandonnerait la boucle et perdrait l'alerte DEFINITIVEMENT                                                                                                                                                                                              | wontfix  | Les deux affirmations porteuses sont fausses. `sendCdpSaturationEmail` ne leve jamais (toutes les branches d'echec de `lib/email/_send.ts` retournent `{ success: false }`), donc la boucle va toujours a son terme et les CDP suivants sont traites. Et `computeChargeAlertes` persiste aussi les REDESCENTES (`__tests__/charge-alertes.test.ts`, « redescente : pas de notification mais re-armement persiste »), donc la perte dure jusqu'au prochain franchissement descendant, pas eternellement. Ne pas re-remonter sous cette formulation. Reste vrai, et non tranche ici : le retour de `sendCdpSaturationEmail` n'est pas teste (ligne ~263), un echec Resend reste silencieux.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Date | Constat | Decision | Raison |
|---|---|---|---|
| 2026-08-10 | `planeFetch` (`lib/plane/client.ts:30`) n'impose aucun timeout, donc une instance Plane muette bloquerait `/accueil` en 504 et rendrait l'application inaccessible | wontfix | Les trois affirmations porteuses sont fausses. Ce n'est pas le seul client sans timeout (`lib/eduvia/quality-client.ts:71`, `lib/process/drive.ts:88`, `lib/crm/email.ts:33`, `lib/brain/github.ts:30` non plus), donc l'argument de rupture d'invariant tombe. `app/(dashboard)/accueil/loading.tsx` existe : le segment est enveloppe d'un Suspense implicite, le shell et la sidebar partent en 200 et restent navigables, le symptome est une carte Taches en squelette, pas un 504. Enfin `PLANE_API_URL` / `PLANE_WORKSPACE` / `PLANE_API_KEY` sont absentes de `.env.example` et de `lib/env.ts`, et `planeConfigured()` court-circuite tout appel : l'activation en prod n'est pas etablie. Reste vrai et non tranche : poser un timeout sur ces cinq clients est un durcissement legitime. |
| 2026-08-10 | `fetchAllPages` (`lib/eduvia/client.ts:398`) ne compare jamais `allItems.length` a `meta.total_count`, donc une reponse tronquee ferait archiver a tort les contrats absents et supprimer des lignes de bordereau | acceptee | Le fait de code est exact, le cadrage non. Si `meta` est absent, `firstPage.meta.total_pages` leve et `safeFetchList` (`lib/eduvia/sync.ts:1429`) re-throw : la sync avorte proprement. Le declencheur exige donc precisement un 200 avec `meta` presente et `total_pages` seul malforme, jamais observe. Et le depot detecte deja ce cas : `app/api/cron/eduvia-audit/route.ts:121-129` leve `orphan_resync_warning` des que le ratio d'orphelins archives depasse 10 % (une troncature a 25 sur ~100 en donnerait 75 %). L'upsert horaire (`sync.ts:719`, `archive: false`) repare le drapeau en moins d'une heure. Reste vrai et non tranche : la perte de rupture via `detectContractChanges` (`sync.ts:618-621`) ne serait pas rattrapee, et une verification de completude en trois lignes fermerait le sujet. |
| 2026-08-10 | Une panne de `listDeliverableStatuses` affiche « 0 % conforme » en rouge sans message, alors que le garde-fou honnete existe dix lignes plus haut | wontfix | La premisse est fausse : 0 % est l'etat NORMAL d'un CFA qui n'a rien depose. `lib/queries/qualiopi-stats.ts:19-22` et `lib/qualiopi/reste-a-faire.ts:47-52` documentent tous deux que le denominateur ne se base pas sur `statuses.length` justement parce qu'Eduvia ne cree une ligne qu'a la premiere evidence. La panne ne fabrique pas un ecran faux, elle reproduit un ecran vrai. Le garde-fou `campuses.length === 0` couvre bien la panne franche (les autres endpoints degradent aussi en vide) ; le residu est une defaillance isolee d'un endpoint sur cinq. Ne pas re-remonter comme « cas rate ». |
| 2026-08-10 | Les 8 tests e2e authentifies se desactivent en silence (`e2e/auth.setup.ts:27-33`) et le job conclut `success` si le secret CI disparait | wontfix | Il n'y a pas de secret : `.github/workflows/e2e.yml:50-54` porte `E2E_ADMIN_EMAIL` et `E2E_ADMIN_PASSWORD` en clair au niveau job, avec le commentaire expliquant que le compte est local. Les faire disparaitre suppose d'editer un fichier versionne, pas un incident. `scripts/e2e-bootstrap.ts` echoue bruyamment (`process.exit(1)` sur env manquant, URL non locale, erreur GoTrue) et c'est une etape bloquante du workflow. Cas croise decisif : identifiants presents mais compte absent, `auth.setup` echoue au lieu de skipper. Reste vrai : en local, `npx playwright test` sans `.env.test.local` rend un run vert dont les 8 tests authentifies sont sautes. |
| 2026-08-10 | `createFactures` (`lib/actions/factures/brouillon-echeancier.ts:242`) n'est couverte par aucun test, comme `createBrouillonEcheancier` et `sendFacturesBulk` | wontfix | `createFactures` est du code mort : ses seules occurrences hors definition sont deux re-exports (`lib/actions/factures/index.ts:7`, `brouillons.ts:15`) et deux commentaires ; le module se decrit lui-meme comme « legacy, table echeances » (`brouillons.ts:5`). C'est deja la refutation de l'audit #122. « Non teste » n'est pas ici un constat de couverture mais un export mort a supprimer. Les deux autres sont des enveloppes minces au-dessus de coeurs testes (`computeEcheancierDues` couvert par `__tests__/echeancier-dues.test.ts`, `sendFacture` par `__tests__/factures-gapless.test.ts`). Reste vrai : la boucle de `sendFacturesBulk` (continuation apres echec, agregat partiel) n'a aucun test. |
| 2026-08-10 | La « source unique » de la convention HT/TTC (`lib/utils/convention-commission.ts`) est contournee par `productionSoluviaHt` (`lib/utils/montant-ht.ts:43`), donc basculer `echeancier` a `false` ferait diverger deux ecrans de 20 % | acceptee | Le comptage est exact (1 appelant contre 3) mais le scenario est impossible : `lib/queries/production.ts:120` appelle `commissionSoluviaHt(npec, taux)` avec DEUX arguments, le parametre `modele` par defaut a `'engagement'`, et `computeContractSchedule` n'a pas de modele dans sa signature. Aucun chemin de production ne lit donc jamais `COMMISSION_EST_TTC.echeancier` : la bascule prescrite en `:65-69` n'a aucun effet, hors un test rouge. Aucun bug actif, les deux formules donnent le meme resultat aujourd'hui. Ne pas re-remonter sous la forme « deux conventions divergentes ». Reste vrai et remonte separement : l'interrupteur est inerte, et c'est la ligne `engagement` qui gouverne les 4 chemins. |
| 2026-08-10 | Cinq definitions concurrentes du « contrat rompu / abandon », dont quatre ignorent l'etat `RUPTURE` | wontfix | Deux erreurs de fait. `isContratRompu('RUPTURE')` renvoie true, la comparaison etant insensible a la casse (`lib/utils/contrat-states.ts:89-91`, verrouille par `__tests__/contrat-states.test.ts:77`). Et les jeux ne mesurent pas la meme chose : `RUPTURES` (`lib/contrats/categories.ts:38`) est une case d'une partition d'affichage a 5 cases, dont la separation rupture/annulation est une demande explicite du commanditaire (`categories.ts:1-8`) ; `ABANDON_STATES` est un ratio qualite dont la formule est AFFICHEE a l'utilisateur (`lib/queries/projet-performance.ts:246-256`, « contrats annules ou resilies / total contrats »). Le classement de `suspendu` dans « Ruptures » est teste comme volontaire (`__tests__/contrats-categories.test.ts:38-43`). Les deux ecrans concernes sont sur deux pages differentes. Reste vrai et remonte separement : `taux_abandon` ne compte pas les contrats en `RUPTURE`. |
| 2026-08-10 | `FENETRE_FACTURATION_FIN = 3` alors que le cron de fermeture part le 2 : incoherence deja presente | wontfix | Coherent et documente deux fois. `app/api/cron/email-fenetre-fin/route.ts:10` (« Sent on the 2nd of each month: the billing window closes tomorrow (3rd) ») et `lib/email/notifications.ts:256` (« 2 du mois = avant-dernier jour »). `FIN = 3` designe le jour de cloture, pas le jour d'envoi ; le rappel « ferme demain » le 2 est exact. Seul flottement : le sujet « Dernier jour pour facturer » (`notifications.ts:288`) envoye l'avant-dernier jour. Reste vrai et remonte separement : `FENETRE_FACTURATION_DEBUT` n'a aucun lecteur, et les parametres admin `facturation.fenetre_debut` / `fenetre_fin` sont editables et jamais lus. |
| 2026-08-10 | `CONTRACT_STATE_COLORS` (`lib/utils/contrat-states.ts:21-33`) fait partie du code mort du depot | wontfix | Faux positif. La constante est lue treize lignes plus bas par `getContratStateColor` (`contrat-states.ts:46`), elle-meme appelee par trois composants montes : `components/projets/projet-progression-table.tsx:39`, `components/projets/contrat-detail-sections.tsx:139`, `components/projets/projet-contrats-categories.tsx:127`. Ne pas la supprimer. |
| 2026-08-10 | Les crons `a-facturer-notif` et `echeancier-retard` perdent leur deduplication par troncature PostgREST a `max_rows` (1000) | wontfix | Le seuil est hors d'atteinte. Le `select` ramene au plus `liens x destinataires` : `selectContratsAFacturer` materialise une ligne par contrat (~100 au repere du depot, migration `20260805120000`), les destinataires sont les superadmins actifs plus le CDP, soit une poignee sur une equipe d'une dizaine. Il faudrait environ 333 contrats dus pour atteindre 1000 a 3 destinataires, soit plus que le total des contrats en base. Reste vrai et remonte separement, sous une autre formulation : l'erreur du `select` n'est pas verifiee, ce qui reinsere le lot une fois, sans filet en base ni a l'affichage. |
| 2026-08-07 | `buildContratIdMap` (`lib/eduvia/sync.ts`) lit `contrats` sans pagination ni chunk, contrairement a sa soeur `snapshotExistingContrats` : progressions et jalons de facturation seraient perdus en silence au-dela de 1000 contrats pour un seul client | acceptee | Le fait de code est exact, ses consequences non. Le seuil (plus de 1000 contrats pour UN client) vaut environ 10 fois la base entiere (« 4 contrats rompus sur ~100 », migration `20260805120000`) et 20 fois le plus gros tenant reel (ids Eduvia 1 a 58). Contrairement a `snapshotExistingContrats`, l'erreur EST correctement remontee ici (`result.errors` + abandon du client), et les donnees concernees sont de purs miroirs re-upsertes a chaque run, donc aucune perte definitive. A rouvrir seulement si un client depasse reellement 1000 contrats. |
| 2026-08-07 | Alertes de saturation CDP (`app/api/cron/passation-echeances/route.ts`) : le palier `cdp_seuil_alerte` est persiste avant l'envoi, donc un echec d'envoi abandonnerait la boucle et perdrait l'alerte DEFINITIVEMENT | wontfix | Les deux affirmations porteuses sont fausses. `sendCdpSaturationEmail` ne leve jamais (toutes les branches d'echec de `lib/email/_send.ts` retournent `{ success: false }`), donc la boucle va toujours a son terme et les CDP suivants sont traites. Et `computeChargeAlertes` persiste aussi les REDESCENTES (`__tests__/charge-alertes.test.ts`, « redescente : pas de notification mais re-armement persiste »), donc la perte dure jusqu'au prochain franchissement descendant, pas eternellement. Ne pas re-remonter sous cette formulation. Reste vrai, et non tranche ici : le retour de `sendCdpSaturationEmail` n'est pas teste (ligne ~263), un echec Resend reste silencieux. |
| Date       | Constat                                                                                                                                                                                                                                                                               | Decision | Raison                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-08 | `getProjetFinanceDetail` (`lib/queries/projet-finance-detail.ts`) : le « Retard d'encaissement » du flux commission somme `montant_ht` brut sans deduire ni les avoirs emis lies ni les paiements encaisses, contrairement a la « DEFINITION UNIQUE » de `lib/utils/avoir-netting.ts` | reportee | Verifie et confirme (le chemin d'emission d'un avoir ne change jamais le statut de la facture d'origine, il n'existe pas de statut `annulee`, et un paiement partiel laisse la facture en `emise`/`en_retard`). Porte par un commentaire de l'issue #143 du 2026-08-08. Non corrige en PR automatique : touche une regle de calcul de facturation, exclue par le prompt de la routine.                                                                                                                                                                                                                                                                                                    |
| 2026-08-08 | `app/api/cron/contrats-archivage/route.ts` : `reglesRes.error` et `contratsRes.error` ne sont pas verifies, une panne de lecture repond `{ success: true, archives: 0 }` en HTTP 200                                                                                                  | wontfix  | Le fait de code est exact, sa gravite non. Contrairement au cron du rapport mensuel (#148), il n'y a ici ni envoi de mail ni verrou d'idempotence : la sortie anticipee n'ecrit rien, la regle d'eligibilite est monotone dans le temps (`jours > delai_jours`), donc le run du lendemain rattrape integralement. Aucune supervision ne lit ce `success` (absent de `/admin/syncs`). Le `?? []` va ici dans la direction fail-safe : il n'archive RIEN, alors que celui de `facture_lignes` — correctement garde par un 500 — archiverait a tort. Ne pas re-remonter comme bug ; reste une amelioration de proprete legitime.                                                             |
| 2026-08-08 | `dateLimiteRetention` (`lib/email/retention.ts`) : `setUTCMonth` deborde sur les mois courts, la purge de `emails_envoyes` supprimerait des lignes trop recentes                                                                                                                      | wontfix  | Le debordement est reel mais la retention livree vaut 24 mois (migration `20260807110000`), un multiple de 12 : reculer de 24 mois retombe sur le meme quantieme, sans debordement. Balayage de 40 ans : 10 dates decalees sur ~14 600, toutes des 29 fevrier, toutes de 1 jour exactement. Soit 24 h de METADONNEES de mails (jamais de corps) tous les 4 ans, au bord d'une fenetre qu'aucun document ne definit a la journee pres. Le parametre n'est modifiable par aucune UI. A rouvrir seulement si une retention non multiple de 12 est introduite.                                                                                                                                |
| 2026-08-07 | `buildContratIdMap` (`lib/eduvia/sync.ts`) lit `contrats` sans pagination ni chunk, contrairement a sa soeur `snapshotExistingContrats` : progressions et jalons de facturation seraient perdus en silence au-dela de 1000 contrats pour un seul client                               | acceptee | Le fait de code est exact, ses consequences non. Le seuil (plus de 1000 contrats pour UN client) vaut environ 10 fois la base entiere (« 4 contrats rompus sur ~100 », migration `20260805120000`) et 20 fois le plus gros tenant reel (ids Eduvia 1 a 58). Contrairement a `snapshotExistingContrats`, l'erreur EST correctement remontee ici (`result.errors` + abandon du client), et les donnees concernees sont de purs miroirs re-upsertes a chaque run, donc aucune perte definitive. A rouvrir seulement si un client depasse reellement 1000 contrats.                                                                                                                           |
| 2026-08-07 | Alertes de saturation CDP (`app/api/cron/passation-echeances/route.ts`) : le palier `cdp_seuil_alerte` est persiste avant l'envoi, donc un echec d'envoi abandonnerait la boucle et perdrait l'alerte DEFINITIVEMENT                                                                  | wontfix  | Les deux affirmations porteuses sont fausses. `sendCdpSaturationEmail` ne leve jamais (toutes les branches d'echec de `lib/email/_send.ts` retournent `{ success: false }`), donc la boucle va toujours a son terme et les CDP suivants sont traites. Et `computeChargeAlertes` persiste aussi les REDESCENTES (`__tests__/charge-alertes.test.ts`, « redescente : pas de notification mais re-armement persiste »), donc la perte dure jusqu'au prochain franchissement descendant, pas eternellement. Ne pas re-remonter sous cette formulation. Reste vrai, et non tranche ici : le retour de `sendCdpSaturationEmail` n'est pas teste (ligne ~263), un echec Resend reste silencieux. |

<!-- Ajoute tes lignes au-dessus de ce commentaire, sous l'en-tete du tableau. -->
