export default function ParametresPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Parametres</h1>
      <p className="text-muted-foreground mt-2 text-sm">
        Configuration du systeme — a implementer (admin uniquement)
      </p>
    </div>
  );
}
